<h1>User info</h1>
<!-- ユーザーイメージの記述をする。attachment? -->
<table>

  <thead>
    <tr>
      <th>name</th>
      <th><%= user.name %></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td>introduction</td>
      <td><%= user.introduction %></td>
    </tr>
  </tbody>
</table>

<!-- edit画面に繋がるスパナマークのeditボタンの記述 -->
<%= link_to 'edit', '' %>



<h1>New book</h1>

<%= form_for(book) do |f| %>

  <% if book.errors.any? %>
      <%= book.errors.count %> errors prohibited this book from being saved:<br>
  <ul>
    <% book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>

    <h4>Title</h4>
      <%= f.text_field :title %>
    <h4>Opinion</h4>
      <%= f.text_area :body %>
      <%= f.submit "Create Book" %>

<% end %>
